  <% if @job_application.errors.any? %>
  <div class="alert alert-danger" role="alert">
    <p>La création de la candidatures n'a pu aboutir pour les raisons suivantes :</p>
    <ul>
      <% @business.errors.full_messages.each do |message| %>
        <li> <%= message %> </li>
      <% end %>
    </ul>
  </div>
<% end %>

<%= form_with model: [@user, @job_application], local: true do |form| %>

  <div class="form-group row">
    <div class="col-lg-3"> <%= form.label :title, "Titre" %> </div>
    <div class="col-lg-9">
      <%= form.text_field :title, class: "form-control"  %>
    </div>
  </div>

  <div class="form-group row">
    <div class="col-lg-3"> <%= form.label :url, "Lien" %> </div>
    <div class="col-lg-9">
      <%= form.text_field :url, readonly: true,  class: "form-control"  %>
    </div>
  </div>

  <div class="form-group row">
    <div class="col-lg-3"> <%= form.label :description, "Description" %> </div>
    <div class="col-lg-9">
      <%= form.text_area :description, class: "form-control", rows: 18 %>
    </div>
  </div>

  <div class="form-group row">
    <div class="col-lg-3">  </div>
    <div class="col-lg-9">
      <%= form.submit "Valider", class: "btn btn-outline-success btn-block col-lg-6" %>
    </div>
  </div>

<% end %>
